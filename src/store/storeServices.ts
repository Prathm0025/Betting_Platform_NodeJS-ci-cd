import axios from "axios";
import createHttpError from 'http-errors';


class StoreService {
    public selectBookmaker(bookmakers: []) {
        console.log("Bookmakers : ", bookmakers);

    }
}

export default StoreService